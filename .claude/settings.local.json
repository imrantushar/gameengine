{
  "permissions": {
    "allow": [
      "Bash(xargs grep -h \"from.*utils/ui\")",
      "Bash(sed 's/^ *//')",
      "Bash(sed 's/ *$//')",
      "Bash(xargs grep -l \"from.*@GFComponents/UI\")",
      "Bash(openspec status *)",
      "Bash(openspec instructions *)"
    ]
  }
}
